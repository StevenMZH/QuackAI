servers:
  host-server:
    server_name: host-server
    server_credentials: .env.hermes
    env: .env.dev

    # apps    
    apps:
      Server-Playground:
        app_name: QuackAI 
        repository: https://github.com/StevenMZH/QuackAI.git
        branch: main
        env: .env.dev
        services: "docker-compose.yml"